[variables]
NIXPACKS_NODE_VERSION = "22.14.0"

[phases.setup]
aptPkgs = ["python3", "build-essential"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run server"
