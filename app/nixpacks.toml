[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[variables]
NIXPACKS_NODE_VERSION = "22.13.0"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run server"
